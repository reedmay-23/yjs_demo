# 项目介绍

## 项目定位

这是一个基于 NestJS、Yjs、WebSocket、Prisma 和 PostgreSQL 的多人协同文档编辑后端项目。

它不是单纯的文档 CRUD 系统，而是围绕“多人实时编辑”这个核心场景，实现了用户鉴权、文档权限、协同房间、Yjs 状态同步、内容快照持久化、历史版本记录、在线版本回退和在线状态维护。

面试时可以把它描述为：

> 一个简化版在线文档协同编辑后端。用户可以创建文档、邀请协作者，不同角色拥有不同权限。多人同时编辑时，通过 Yjs 解决并发编辑冲突，通过 WebSocket 实时同步，服务端负责鉴权、房间管理、在线状态、文档快照持久化、历史版本记录和版本回退。

## 项目难度判断

这个项目放在简历上是可以的，难度高于普通后台管理系统和常规 CRUD 项目。

主要原因：

- 涉及 WebSocket 长连接，不只是 HTTP 接口。
- 涉及 Yjs CRDT 协同编辑，不是简单文本覆盖。
- 涉及 HTTP 和 WebSocket 两套鉴权入口。
- 涉及文档级权限控制。
- 涉及内存房间生命周期管理。
- 涉及协同状态持久化和恢复。
- 涉及在线协作者状态维护。
- 涉及历史版本合并、回退审计和在线房间失效处理。

如果面试初中级前端或后端岗位，这个项目有比较强的讲解价值。如果面试更高级岗位，需要主动补充它目前的边界和后续优化方向，比如多实例部署、可靠持久化、安全加固和测试覆盖。

## 简历写法

项目名称：

> 基于 Yjs 的多人协同文档编辑系统

项目描述：

> 基于 NestJS、Prisma、PostgreSQL、WebSocket 和 Yjs 实现多人协同文档编辑后端，支持用户登录、文档管理、协作者权限、实时同步、在线状态、文档快照持久化、历史版本回退和统一异常响应。

职责描述可以写：

- 设计用户、文档、协作者、协同会话等数据模型，使用 Prisma 管理 PostgreSQL 数据。
- 基于 JWT 实现 HTTP 与 WebSocket 双通道鉴权，防止用户伪造身份访问他人文档。
- 基于 Yjs 实现多人协同编辑，服务端维护每个文档的内存房间并转发增量更新。
- 实现文档内容快照持久化，支持服务重启后从数据库恢复 Y.Doc 状态。
- 设计文档历史记录和回退机制，自动编辑历史按用户和时间窗口合并，回退操作单独审计。
- 支持在线版本回退，回退后主动关闭协同房间连接，要求前端重建 Tiptap/Y.Doc 后重新加载回退快照。
- 实现 owner / editor / viewer 权限模型，区分只读访问、编辑访问和协作者管理。
- 实现在线协作者状态维护，记录用户进入和离开文档的会话状态。
- 通过防抖保存降低高频协同编辑场景下的数据库写入压力。

## 面试讲解顺序

### 1. 项目背景

可以这样讲：

> 这个项目是一个多人协同文档编辑系统，目标类似简化版在线文档。用户可以创建文档，邀请协作者，不同角色有不同权限。多人同时编辑时，内容通过 Yjs 做冲突合并，通过 WebSocket 实时同步，服务端负责鉴权、房间管理、在线状态和内容持久化。

### 2. 整体架构

可以这样讲：

> 项目后端使用 NestJS 分模块组织，主要有 Auth、Document、YjsStorage、WebSocket Gateway、Prisma 模块。HTTP 接口负责登录、文档管理、协作者管理；WebSocket 负责协同编辑实时通信；Prisma 负责数据库访问；YjsStorageService 是文档状态、权限校验和持久化的核心服务。

核心模块：

- `AuthModule`：注册、登录、token 刷新、获取当前用户。
- `DocumentModule`：文档创建、列表、详情、删除、协作者管理、历史列表和版本回退。
- `YjsStorageService`：Y.Doc 编码、解码、保存、恢复、权限校验、在线会话管理、历史记录合并和回退事务。
- `YjsRoomEventsService`：房间事件服务，当前用于回退后通知 WebSocket 网关关闭并丢弃旧房间。
- `YjsCollabGateway`：自定义 Yjs WebSocket 协同服务。
- `PrismaModule`：数据库访问。

### 3. 核心业务流程

可以这样讲：

> 用户登录后拿到 access token，然后创建或打开文档。前端通过 WebSocket 连接 `/collab1?docId=xxx&accessToken=xxx`。服务端先校验 token，再用 token 中的 userId 判断用户是否有该文档写权限。校验通过后，服务端为该 docId 创建或复用内存中的 Y.Doc 房间，从数据库恢复历史快照，然后和客户端进行 Yjs sync 协议同步。之后客户端本地编辑产生 Yjs update，服务端收到后广播给同房间其他连接，并防抖保存快照到数据库，同时按时间窗口维护历史版本。

## 要讲解的重点

### 1. 为什么用 Yjs

可以这样讲：

> 普通 WebSocket 只能传输消息，不能解决多人同时编辑的冲突问题。比如两个人同时在同一位置插入或删除文本，如果自己设计合并逻辑会很复杂。Yjs 是 CRDT 实现，可以把每个客户端的操作表示成可合并的 update，即使操作顺序不同，最终状态也能收敛一致。所以 WebSocket 在这里主要负责传输，冲突合并交给 Yjs。

补充说明：

> 服务端保存的是 Y.Doc 的二进制 update 快照，而不是简单保存纯文本，这样能保留 Yjs 的协同状态。

### 2. WebSocket 鉴权

可以这样讲：

> WebSocket 连接不能只信任前端传来的 userId，因为 userId 很容易伪造。所以我在连接初始化时只读取 access token，通过 JWT 校验拿到 payload.sub 作为真实用户 ID。然后根据 docId 查询文档和协作者表，判断用户是否有权限。没有 token、token 过期、文档不存在、无权限都会直接关闭连接。

实现点：

- `verifyWsAccessToken()` 负责从 query 或 header 取 token 并校验。
- `validateDocumentAccess()` 负责判断 owner / editor / viewer 权限。
- WebSocket 连接建立前先鉴权，避免非法用户进入协同房间。

### 3. 权限模型

可以这样讲：

> 权限分三类：owner、editor、viewer。owner 是文档创建者，可以删除文档、管理协作者；editor 可以进入协同编辑；viewer 只能读取文档内容，不能进入写协同房间。权限不是靠前端按钮控制，而是在后端每个关键接口和 WebSocket 连接入口都校验。

数据设计：

- `Document.createdBy` 表示文档 owner。
- `DocumentCollaborator.role` 表示协作者角色。
- `validateDocumentAccess(docId, userId, 'read' | 'write')` 统一校验访问级别。

### 4. 协同房间管理

可以这样讲：

> 服务端按 docId 维护一个内存 room，每个 room 里有一个 Y.Doc、awareness、连接列表和定时器。第一个用户进入时从数据库恢复快照，后续用户进入复用同一个 room。用户编辑产生 update 后，服务端广播给同房间其他客户端，并触发防抖持久化。房间无人后不会立刻销毁，而是延迟清理，清理前再保存一次快照，减少频繁打开关闭带来的初始化成本。

重点词：

- 按文档隔离房间。
- 复用内存 Y.Doc。
- 防抖持久化。
- 无人后延迟释放。
- 清理前保存快照。

### 5. 文档持久化

可以这样讲：

> Yjs 的 update 是二进制数据，数据库里 JSON 字段不方便直接存 Uint8Array，所以我把 `Y.encodeStateAsUpdate(doc)` 转成 `number[]` 保存到 `documents.content`。恢复时再转回 Uint8Array，然后 `Y.applyUpdate()` 应用到新的 Y.Doc。每次保存时 version 自增，方便做版本追踪、历史记录和回退审计。

实现点：

- `createEmptyState()` 创建空 Y.Doc 快照。
- `encodeState()` 把 Y.Doc 编码成 `number[]`。
- `decodeState()` 从数据库恢复 `Uint8Array`。
- `saveSnapshot()` 保存内容并让 `version` 自增。

### 6. 历史记录和版本回退

可以这样讲：

> 项目里把“自动保存”和“历史版本”分成两层。`documents.content` 保持 1 秒防抖保存，保证当前内容尽量新；`document_histories` 用来做用户可见的版本记录。为了避免用户每敲几下就生成一条历史，同一文档、同一用户 5 分钟内的 `edit` 历史会合并，只更新最新完整 Y.Doc 快照。回退操作不合并，每次都会新增一条 `rollback` 记录，记录是谁在什么时候回退到了哪个来源版本。

实现点：

- `DocumentHistory.content` 保存完整 Y.Doc 快照，不保存纯文本或单次增量。
- `edit` 历史按“同一文档 + 同一用户 + 5 分钟窗口”合并。
- `rollback` 历史每次新增，保留 `sourceVersion`。
- 历史列表只返回元数据和 `contentSize`，不把完整快照返回给列表 UI。

在线回退可以这样讲：

> Tiptap/Yjs 场景下不能简单把旧快照 apply 到当前 Y.Doc，因为 Yjs 会偏向合并，当前新增内容不一定被删除。为了保证所有在线用户看到回退后的内容，回退成功后服务端会关闭该文档房间里的 WebSocket，关闭码是 `4409`，reason 是 `document_rolled_back`，并丢弃旧的内存 Y.Doc。前端收到这个关闭事件后必须销毁当前 Tiptap editor 和本地 Y.Doc，重新创建并连接，重新连接时会从数据库加载回退后的快照。

### 7. 在线协作者状态

可以这样讲：

> 在线状态由 `collaboration_sessions` 表维护。用户进入文档时 upsert 一条 session，设置 `isActive = true` 和 `lastSeen`；断开连接时设置为 false。为了处理同一用户多标签页打开同一文档，我在内存中维护了连接引用计数，只有最后一个连接断开时才真正标记离线。

## 项目难点

### 难点一：多人同时编辑冲突

难点：

> 多人同时插入、删除、撤销时，如果直接同步完整文本，很容易互相覆盖。

解决：

> 使用 Yjs CRDT，把编辑操作变成可合并 update，服务端负责转发和持久化，不自己手写冲突合并算法。

### 难点二：WebSocket 鉴权

难点：

> WebSocket 不像普通 HTTP 请求那样天然走全局 Guard，连接建立阶段必须自己处理 token 校验。

解决：

> 在 Gateway 的连接入口解析 access token，只信任 JWT payload，不信任前端传来的 userId。

### 难点三：权限和协同协议结合

难点：

> 普通接口的权限和协同连接的权限必须一致，否则可能出现接口无权限但 WebSocket 可以写入的问题。

解决：

> 抽出 `validateDocumentAccess`，HTTP 读写和 WebSocket 连接都复用同一套校验。

### 难点四：Y.Doc 持久化

难点：

> Yjs 内部状态不是普通字符串，不能简单保存文本，否则会丢失协同状态。

解决：

> 使用 `encodeStateAsUpdate` 存快照，恢复时使用 `applyUpdate`。

### 难点五：在线状态准确性

难点：

> 同一用户可能多标签页连接同一个文档，不能一个标签页关闭就显示用户离线。

解决：

> 使用 `activeConnectionRefs` 维护用户在某文档下的连接计数，最后一个连接断开时才标记离线。

### 难点六：性能和资源释放

难点：

> 每次编辑 update 都写数据库会造成写入压力，房间长期不释放也会占用内存。

解决：

> 使用 1 秒防抖保存，并在房间无人后延迟清理，清理前保存最终快照。

### 难点七：Tiptap 在线回退

难点：

> 富文本协同编辑不是简单字符串替换。Tiptap 的协同状态存在 Yjs 结构里，如果直接向在线客户端广播历史快照，Yjs 可能会把旧版本和当前版本合并，无法得到真正的“回退”效果。

解决：

> 把在线回退设计成“房间失效 + 客户端重连”。回退事务先更新数据库快照和历史记录，然后通过房间事件通知网关关闭该文档所有连接，丢弃旧内存房间。客户端收到 `4409 document_rolled_back` 后销毁 Tiptap editor 和 Y.Doc，重新连接并加载数据库里的回退快照。

## 常见面试问题和回答

### Q1：这个项目最大的亮点是什么？

A：

> 最大亮点是把 Yjs 协同编辑和后端权限体系结合起来了。不是单纯转发 WebSocket 消息，而是实现了文档级鉴权、协作者角色、在线会话、Y.Doc 快照持久化和房间生命周期管理。

### Q2：为什么选择 Yjs？

A：

> 因为多人协同编辑的核心问题是冲突合并。Yjs 基于 CRDT，可以保证多个客户端并发编辑后最终一致。相比自己实现 OT 或手写文本 diff 合并，Yjs 更成熟，也更适合和富文本编辑器集成。

### Q3：服务端是否参与冲突解决？

A：

> 不直接参与业务层面的冲突解决。冲突合并由 Yjs 完成。服务端主要维护每个文档的 Y.Doc 状态，负责接收 update、广播 update、保存快照和恢复快照。

### Q4：为什么数据库里不直接存文本？

A：

> 如果只存文本，可以展示内容，但会丢失 Yjs 的协同状态。项目里保存的是 Y.Doc 编码后的 update 快照，恢复后可以继续参与协同同步。只读接口需要纯文本时，再临时恢复 Y.Doc 并读取 `Y.Text`。

### Q5：WebSocket 如何鉴权？

A：

> 连接时前端带 access token，后端用 JWT secret 验证 token，取 payload.sub 作为真实用户 ID。然后根据 docId 查询文档 owner 和协作者角色。只有 owner/editor 可以进入写协同连接，viewer 只能通过 HTTP 只读接口读取内容。

### Q6：如果用户伪造 userId 怎么办？

A：

> 后端不信任前端传来的 userId。WebSocket 和 HTTP 都以 token 中的 userId 为准，所以伪造 query 参数没有意义。

### Q7：多人同时编辑，消息丢失怎么办？

A：

> Yjs 同步协议里有 state vector 和 sync step 机制。新客户端连接时会和服务端交换状态，缺失的 update 可以通过同步过程补齐。服务端还会在连接建立时主动发送当前快照，保证新用户能拿到历史内容。

### Q8：为什么要做防抖保存？

A：

> 编辑过程中 Yjs update 很频繁，如果每次都立即写数据库，会造成大量写压力。项目里收到 update 后 1 秒防抖保存，减少数据库写入次数。同时房间销毁前会再保存一次，降低数据丢失风险。

### Q8.1：历史记录会不会太多？

A：

> 不会每次自动保存都新增历史。当前文档内容仍然 1 秒防抖保存，但用户可见的 `edit` 历史会按同一文档、同一用户、5 分钟窗口合并。合并时保存的是最新完整 Y.Doc 快照，所以不会影响回退完整性，只是降低历史粒度。回退记录不合并，每次都单独记录，方便审计。

### Q9：如果服务突然宕机，会不会丢数据？

A：

> 当前实现是防抖异步保存，所以理论上可能丢失最近 1 秒内尚未落库的 update。可以优化为增量 update 日志、Redis Stream、消息队列或更短间隔的 append-only 存储，再定期合并快照。

注意：不要回答“完全不会丢”，这个回答不严谨。

### Q10：项目是否支持多实例部署？

A：

> 当前主要是单实例内存房间模型。多实例部署时，不同用户可能连到不同实例，内存 Y.Doc 不共享，会导致同步问题。在线回退的房间失效事件目前也是进程内事件，多实例时也需要广播到所有实例。生产级方案可以引入 Redis Pub/Sub、消息队列，或者使用 y-redis/y-websocket 的分布式方案，让不同实例之间广播 Yjs update 和房间失效事件。

### Q11：viewer 为什么不能进入协同 WebSocket？

A：

> 因为当前 `/collab1` 是写协同通道，只要进入房间就可能发送 Yjs update。为了安全，viewer 只允许走 HTTP 只读接口读取内容。如果要支持只读实时预览，可以单独设计 read-only WebSocket，只接收广播，不处理客户端 update。

### Q12：在线用户怎么统计？

A：

> 用户连接文档时 upsert collaboration session，断开时标记离线。为了避免同一个用户多个标签页导致状态错误，服务端维护连接引用计数，只有该用户在该文档下所有连接都断开，才把 session 标记为离线。

### Q13：这个项目还有哪些不足？

A：

> 目前还有几个可以优化的地方。第一，密码应该用 bcrypt 哈希存储，不能明文。第二，单实例内存房间和进程内回退事件不适合直接水平扩展。第三，持久化是快照防抖保存，极端宕机可能丢最近一次防抖窗口内的数据。第四，自动化测试需要补充 WebSocket 鉴权、权限边界、在线回退和协同同步场景。

### Q14：如果让你继续优化，会怎么做？

A：

> 我会从四个方向优化：安全上改 bcrypt、token 日志脱敏和 refresh token 哈希存储；可靠性上增加 Yjs update 增量日志，定期压缩快照；扩展性上引入 Redis Pub/Sub 支持多实例广播 Yjs update 和回退房间失效事件；工程质量上补充 e2e 测试和 README/API 文档。

## 推荐开场白

面试时可以这样开场：

> 这个项目是一个基于 Yjs 的多人协同文档编辑后端。普通 CRUD 部分包括用户、文档和协作者管理，但核心难点在实时协同。用户通过 JWT 登录后，HTTP 接口负责创建文档和配置权限，WebSocket 连接负责同步 Yjs 二进制 update。服务端按 docId 维护内存房间，从数据库恢复 Y.Doc 快照，收到编辑 update 后广播给其他客户端，并做防抖持久化。同时我做了文档级权限校验，owner/editor/viewer 角色区分、历史版本记录、在线版本回退，以及在线会话状态维护。

可以用这句话收尾：

> 所以这个项目重点不是“做了文档 CRUD”，而是“把协同编辑协议、权限控制、实时连接和持久化串成了一个完整闭环”。

## 面试前建议补强

建议至少处理下面几个点：

1. 把密码逻辑改成 bcrypt 哈希存储。
2. 修复代码和接口文档里的中文乱码。
3. 补充 WebSocket 鉴权、文档权限、协作者管理相关测试。
4. 给 README 增加启动流程、架构说明和协同流程。
5. 准备好说明当前项目还不是完整生产级多实例架构。
